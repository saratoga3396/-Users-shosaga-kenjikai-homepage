# Vercelでの独自ドメイン設定手順

私は直接あなたのVercelアカウントを操作することができないため、以下の手順で設定をお願いします。

## 1. Vercelダッシュボードでの設定

1.  [Vercel Dashboard](https://vercel.com/dashboard) にログインします。
2.  対象のプロジェクト（`kenjikai-homepage`）を選択します。
3.  上部メニューの **Settings** をクリックします。
4.  左サイドバーから **Domains** を選択します。
5.  入力フィールドに `kenjikai.com` と入力し、**Add** ボタンをクリックします。
6.  推奨される設定（通常は `kenjikai.com` と `www.kenjikai.com` の両方）が表示されるので、そのまま追加します。

## 2. DNS設定（ドメイン管理会社側）

Vercelの画面に、DNS設定に必要な情報（AレコードやCNAMEレコード）が表示されます。
ドメインを取得したサービス（お名前.com、Google Domains、GoDaddyなど）の管理画面にログインし、以下の設定を追加してください。

### 一般的な設定例（Vercelの表示に従ってください）

| タイプ | ホスト名 (Name) | 値 (Value) |
| :--- | :--- | :--- |
| **A** | `@` (または空欄) | `76.76.21.21` |
| **CNAME** | `www` | `cname.vercel-dns.com` |

※ すでに他の設定がある場合は、競合しないように注意してください。

## 3. 確認

設定後、反映されるまで数分〜最大48時間程度かかる場合があります。
VercelのDomains画面で、ドメインの横に緑色のチェックマーク ✅ が付けば設定完了です。
